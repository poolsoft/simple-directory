<?php
/*
 * Page: directory_guide
 */

global $current_user;
get_currentuserinfo();

?>
<h1>
Welcome to Simple Directory
</h1>
<p>Simple Directory creates a custom post type called a 'Listing'.  Each listing has a title (just like a regular post) and a description. There are fields for contact info and social links.  To add a logo, use the 'Featured Image' uploader.</p>
<h2>Templates</h2>
<p>The plugin comes with two templated for displaying the listings: single-listing.php and archive-listing.php.  Both of these files are found in the /templates/ directory.  You can customize the 
